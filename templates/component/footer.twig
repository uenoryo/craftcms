<hr>

{% set civilizations = craft
    .categories()
    .group('civilization')
    .all() %}

<div class="Footer">
  <h3>カードを探す</h3>
  {% for civ in civilizations %}
    <a href="{{ civ.url }}">{{ civ.title }}</a>
  {% endfor %}
</div>
